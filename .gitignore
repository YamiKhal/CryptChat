# dependencies
node_modules/

# build output
dist/
build/

# encrypted blob store (local dev). In production this is a mounted volume on a
# separate disk from Postgres, never inside the repo or the image.
backend/data/
data/

# env / secrets
.env
.env.example
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
.vscode/
.idea/